{
  "name": "kanban-backend",
  "version": "1.0.0",
  "description": "Kanban Board Backend API",
  "main": "kanban-server.js",
  "scripts": {
    "start": "node kanban-server.js",
    "dev": "nodemon kanban-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["kanban", "task-management", "api", "express"],
  "author": "ProTool Team",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.6.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "prisma": "^5.6.0",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
